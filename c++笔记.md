### c++优先级和结合性

- 从高到底：()  []  ->  .   ++    - -  

- ```c++
  int i = 0;
  i = i++;
  //此时i=0，因为处理时先使用一个临时变量接收i（0）；然后将i自增1，i变为1；最后是把前面那个临时变量（0）赋值给i，所以i又从1变成了0；
  int a = 0;
  a = i++;
  //此时a = 0, i = 1.
  ```

- 